#include "main.h"

/**
 * print_diagsums - print sum a square's two diagonals
 * @a:
 * @size
 * Return: always void
 */
void print_diagsums(int *a, int size)
{
int i, j, k, l;

for (i = 0; i < size; i++)
{
for (j = 0; j < size; j++)
{
if (i == j)
{
sum1 = a[i][j];
}
}
}
for (i = 0; i < size; i++)
{
for (j = 0; j < size; j++)
 
