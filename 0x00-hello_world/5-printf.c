#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int snt;
	snt = "with proper grammar, but the outcome is a piece of art,";
	printf("%s\n",snt);
	return (0);
}
