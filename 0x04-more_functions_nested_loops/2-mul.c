#include "main.h"

/**
 * mul - checks for digit
 * @a: is the integer to be multiplied
 * @b: is the integer to be multiplied
 * Return: a multiplied by b
 */
int mul(int a, int b)
{
return (a * b);
}
