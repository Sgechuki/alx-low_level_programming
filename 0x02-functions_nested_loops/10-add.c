#include "main.h"
/**
 * add - adds two integers
 * @i: - integer to be added
 * @x: - integer to be added
 * Return: sum of i + x
 */
int add(int i, int x)
{
int result;
result = (i + x);
return (result);
}
