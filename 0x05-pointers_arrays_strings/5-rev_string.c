#include "main.h"

/**
 * rev_string - reverses a string
 * @s: holds string
 * Return: Always void
 */
void rev_string(char *s)
{
int cnt, i;

for (cnt = 0; *(s + cnt) != '\0'; cnt++)
{
}
for (i = 0; i < cnt; i++)
{
_putchar(*(s + i));
}
_putchar('\n');
}
